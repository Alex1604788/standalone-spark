# 📋 Инструкция по применению VIEW для Аналитики Продвижения

## Проблема
На странице "Аналитика Продвижения" не отображаются данные, потому что VIEW `ozon_performance_summary` не существует в базе данных.

## Решение

### Шаг 1: Откройте Supabase SQL Editor

Перейдите по ссылке:
👉 https://supabase.com/dashboard/project/bkmicyguzlwampuindff/sql/new

### Шаг 2: Скопируйте SQL миграцию

Откройте файл:
```
supabase/migrations/20260114_create_ozon_performance_summary_view.sql
```

Скопируйте ВЕСЬ текст из этого файла.

### Шаг 3: Примените миграцию

1. Вставьте скопированный SQL в SQL Editor
2. Нажмите кнопку **"Run"** (или Ctrl+Enter)
3. Должно появиться сообщение: **"Success. No rows returned"**

### Шаг 4: Проверьте VIEW

Выполните в SQL Editor:
```sql
SELECT COUNT(*) FROM ozon_performance_summary;
```

Должно вернуться количество записей.

### Шаг 5: Обновите страницу приложения

1. Откройте страницу "Аналитика Продвижения" в приложении
2. Нажмите **Ctrl+F5** (жёсткое обновление)
3. Данные должны появиться!

## Что делает этот VIEW?

VIEW `ozon_performance_summary` агрегирует данные из таблицы `ozon_performance_daily`:
- Группирует по дням и рекламным кампаниям
- Суммирует `orders + orders_model` (как в OZON Analytics)
- Суммирует revenue, показы, клики, расходы
- Вычисляет средние значения CTR, CPC, конверсии, ДРР

## После применения VIEW

После успешного применения миграции:
- ✅ Данные появятся на странице "Аналитика Продвижения"
- ✅ Будут показаны общие метрики (расходы, выручка, ДРПР)
- ✅ Фильтр дат будет работать
- ✅ Последний выбранный период будет сохраняться

## Если проблемы остались

1. Проверьте консоль браузера (F12 → Console)
2. Убедитесь, что вы авторизованы в приложении
3. Проверьте, что marketplace_id корректный
4. Убедитесь, что в таблице `ozon_performance_daily` есть данные:
   ```sql
   SELECT COUNT(*) FROM ozon_performance_daily;
   ```
