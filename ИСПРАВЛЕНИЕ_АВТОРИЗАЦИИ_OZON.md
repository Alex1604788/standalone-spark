# 🔐 ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С АВТОРИЗАЦИЕЙ OZON API

**Дата:** 13 января 2026
**Проблема:** "Failed to obtain access token" - "Client authentication failed"

---

## ❌ ДИАГНОСТИКА ПРОБЛЕМЫ

### Что происходит:

При попытке запустить синхронизацию OZON Performance API возвращает ошибку:
```json
{
  "error": "Failed to obtain access token",
  "details": "{\"error\":\"invalid_client\",\"error_description\":\"Client authentication failed\"}"
}
```

### Что проверено:

✅ API credentials есть в базе данных (Client ID: 1172055...)
✅ Edge Function работает и пытается получить токен
❌ OZON API отклоняет Client ID/Secret

---

## 🔍 ПРИЧИНЫ ПРОБЛЕМЫ

### Возможные причины:

1. **Client ID или Client Secret неверные**
   - Возможно скопированы не полностью
   - Возможно содержат лишние пробелы

2. **Credentials истекли или отозваны**
   - OZON мог автоматически отозвать токен
   - Возможно удалён доступ к API

3. **IP адрес не в белом списке**
   - Если в настройках OZON API настроена IP-фильтрация
   - Edge Function работает с определённых IP Supabase

---

## ✅ РЕШЕНИЕ: Обновить API Credentials

### Шаг 1: Получить новые credentials в OZON

1. Перейдите на: **https://performance.ozon.ru/api**

2. Войдите в аккаунт OZON

3. В разделе **API** найдите или создайте новый Client:
   - Нажмите "Создать клиента" (если нужен новый)
   - Или скопируйте существующий Client ID и Secret

4. **ВАЖНО:** Скопируйте полностью:
   - **Client ID** - длинная строка цифр (например: 1172055)
   - **Client Secret** - длинная строка символов (показывается только при создании!)

5. **Если настроена IP-фильтрация:**
   - Либо отключите её
   - Либо добавьте IP адреса Supabase в белый список

---

### Шаг 2: Обновить credentials в приложении

#### Вариант A: Через UI (Рекомендуется)

1. Откройте в браузере: **https://standalone-spark.lovable.app/app/settings/ozon**

2. В разделе **"Настройка API OZON Продвижения"** найдите поля:
   - Client ID
   - Client Secret

3. Вставьте **новые** значения

4. Нажмите кнопку **"Сохранить"**

5. Дождитесь сообщения "✅ Подключено" (зелёный статус)

---

#### Вариант B: Через Supabase SQL Editor

Если UI не работает, обновите напрямую в базе:

1. Откройте: **https://supabase.com/dashboard/project/bkmicyguzlwampuindff/sql**

2. Выполните SQL:

```sql
UPDATE marketplace_api_credentials
SET
  client_id = 'ВАШ_НОВЫЙ_CLIENT_ID',
  client_secret = 'ВАШ_НОВЫЙ_CLIENT_SECRET',
  access_token = NULL,
  token_expires_at = NULL,
  updated_at = NOW()
WHERE marketplace_id = '84b1d0f5-6750-407c-9b04-28c051972162'
  AND api_type = 'performance';
```

3. Замените:
   - `ВАШ_НОВЫЙ_CLIENT_ID` на реальный Client ID
   - `ВАШ_НОВЫЙ_CLIENT_SECRET` на реальный Client Secret

---

### Шаг 3: Проверить подключение

1. На странице `/app/settings/ozon` нажмите кнопку **"Проверить подключение"**

2. Должно появиться: **"✅ Подключено"** с зелёным индикатором

3. Статус должен измениться с ❌ на ✅

---

### Шаг 4: Запустить синхронизацию

1. На той же странице нажмите кнопку **"Запустить"**
   - Напротив "Полная синхронизация (62 дня)"
   - Или "Последняя синхронизация (7 дней)"

2. Подождите 10-15 минут

3. Обновите страницу и проверьте что:
   - Синхронизация завершилась успешно
   - Дата последней синхронизации обновилась
   - Записей добавлено > 0

---

## 🔍 ПРОВЕРКА ЧЕРЕЗ SQL (опционально)

Если хотите проверить через SQL:

```sql
-- Проверить что credentials обновлены
SELECT
  client_id,
  CASE
    WHEN client_secret IS NOT NULL THEN '***скрыто***'
    ELSE 'НЕТ'
  END as secret_status,
  access_token IS NOT NULL as has_token,
  token_expires_at,
  updated_at
FROM marketplace_api_credentials
WHERE marketplace_id = '84b1d0f5-6750-407c-9b04-28c051972162'
  AND api_type = 'performance';
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### При копировании credentials:

❌ **НЕ делайте:**
- Копировать с пробелами в начале/конце
- Копировать частично (только половину)
- Использовать старые credentials "на всякий случай"

✅ **ДЕЛАЙТЕ:**
- Копировать полностью (весь текст)
- Проверять что нет лишних символов
- Использовать свежесозданные credentials

### Client Secret показывается только один раз!

Когда создаёте нового клиента в OZON:
- Client Secret показывается **ТОЛЬКО ОДИН РАЗ** при создании
- Сохраните его сразу в безопасное место
- Если потеряли - придётся создать нового клиента

---

## 📊 ПОСЛЕ ИСПРАВЛЕНИЯ

После успешного обновления credentials:

✅ Статус подключения: **"✅ Подключено"** (зелёный)
✅ Синхронизация запускается без ошибок
✅ Данные обновляются в таблице `ozon_performance_daily`
✅ VIEW `ozon_performance_summary` показывает актуальные данные

---

## 🆘 ЕСЛИ НЕ ПОМОГЛО

Если после обновления credentials проблема осталась:

1. **Проверьте Client ID:**
   - Это должна быть длинная строка **ТОЛЬКО ЦИФР**
   - Пример: `1172055` (но длиннее)

2. **Проверьте Client Secret:**
   - Это длинная строка символов (буквы, цифры, спецсимволы)
   - НЕ должен содержать кавычек или пробелов по краям

3. **Проверьте IP-фильтрацию в OZON:**
   - Зайдите в настройки API на https://performance.ozon.ru/api
   - Проверьте что IP-фильтрация либо отключена, либо разрешены все IP

4. **Попробуйте создать нового клиента:**
   - Удалите старого клиента в OZON
   - Создайте нового с нуля
   - Скопируйте новые credentials

---

## 📞 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Где хранятся credentials:

**База данных:** `marketplace_api_credentials`
**Таблица:** `marketplace_api_credentials`
**Поля:**
- `client_id` - Client ID от OZON
- `client_secret` - Client Secret от OZON (зашифрован)
- `access_token` - Временный токен (обновляется автоматически)
- `token_expires_at` - Время истечения токена

### Как работает авторизация:

1. Edge Function читает `client_id` и `client_secret` из базы
2. Отправляет запрос на `https://api-performance.ozon.ru/api/client/token`
3. Получает `access_token` (живёт 30 минут)
4. Сохраняет токен в базу для переиспользования
5. Использует токен для всех запросов к OZON API

---

**Удачи с исправлением!** 🚀

После обновления credentials всё должно заработать. Если есть вопросы - смотрите логи Edge Function в Supabase Dashboard → Edge Functions → Logs.
