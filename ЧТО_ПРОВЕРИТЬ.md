# 🔍 Диагностика медленной обработки

## Текущая ситуация:
✅ Функция работает: **37 ответов за 30 минут**
❌ Но это медленно: ожидалось **50-100 за раз мгновенно**

## Возможные причины:

### 1. Используется ИИ вместо шаблонов
- Если `use_templates_X = false` → используется ИИ → медленно (каждый запрос ~2-5 сек)
- Если `use_templates_X = true` → используются шаблоны → быстро (мгновенно)

### 2. Cron job обрабатывает только часть отзывов
- Лимит увеличен до 200, но может быть проблема с запросом
- Проверьте логи функции в Supabase Dashboard

### 3. Некоторые рейтинги в режиме "off"
- Если `reviews_mode_X = 'off'` → отзывы не обрабатываются

## Что проверить:

### Шаг 1: Проверьте настройки
Выполните в SQL Editor:
```sql
SELECT 
  reviews_mode_1, reviews_mode_2, reviews_mode_3, reviews_mode_4, reviews_mode_5,
  use_templates_1, use_templates_2, use_templates_3, use_templates_4, use_templates_5
FROM marketplace_settings;
```

**Должно быть:**
- `reviews_mode_X = 'auto'` или `'semi'` (не `'off'`)
- `use_templates_X = true` (для быстрой обработки)

### Шаг 2: Проверьте логи функции
1. Supabase Dashboard → Edge Functions → auto-generate-drafts → Logs
2. Найдите последние записи:
   ```
   [auto-generate-drafts] Found X unanswered reviews
   [auto-generate-drafts] Completed: Reviews - X drafts, Y scheduled
   ```
3. Проверьте, сколько обрабатывается за запуск

### Шаг 3: Проверьте шаблоны
Выполните в SQL Editor:
```sql
SELECT rating, COUNT(*) as count 
FROM reply_templates 
WHERE deleted_at IS NULL 
GROUP BY rating;
```

**Должно быть:** шаблоны для всех рейтингов (1-5)

## Решение:

Если шаблоны не используются:
1. Включите `use_templates_X = true` в настройках
2. Убедитесь, что шаблоны созданы
3. Перезапустите функцию

Если шаблоны используются, но все равно медленно:
1. Проверьте логи - может быть проблема с запросом
2. Увеличьте частоту cron job (сейчас каждые 5 минут)

