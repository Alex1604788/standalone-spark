# Настройка Автоматического Деплоя Supabase

## 📋 Необходимые GitHub Secrets

Для работы автоматического деплоя нужно настроить следующие секреты в GitHub:

### 1. SUPABASE_ACCESS_TOKEN
**Где получить:**
1. Откройте https://supabase.com/dashboard/account/tokens
2. Создайте новый Access Token
3. Скопируйте токен (он показывается только один раз!)

**Как добавить в GitHub:**
1. Откройте ваш репозиторий на GitHub
2. Перейдите в `Settings` → `Secrets and variables` → `Actions`
3. Нажмите `New repository secret`
4. Name: `SUPABASE_ACCESS_TOKEN`
5. Secret: вставьте токен из Supabase
6. Нажмите `Add secret`

### 2. SUPABASE_PROJECT_ID
**Где получить:**
1. Откройте https://supabase.com/dashboard/projects
2. Выберите ваш проект
3. В Settings → General найдите **Reference ID** (например: `abcdefghijklmnop`)

**Как добавить в GitHub:**
1. `Settings` → `Secrets and variables` → `Actions`
2. Нажмите `New repository secret`
3. Name: `SUPABASE_PROJECT_ID`
4. Secret: вставьте Reference ID
5. Нажмите `Add secret`

### 3. SUPABASE_DB_PASSWORD
**Где получить:**
1. Откройте https://supabase.com/dashboard/project/YOUR_PROJECT/settings/database
2. Найдите **Database password** (пароль, который вы задали при создании проекта)
3. Если забыли пароль - можно сбросить через "Reset database password"

**Как добавить в GitHub:**
1. `Settings` → `Secrets and variables` → `Actions`
2. Нажмите `New repository secret`
3. Name: `SUPABASE_DB_PASSWORD`
4. Secret: вставьте пароль от БД
5. Нажмите `Add secret`

## 🚀 Как работает автодеплой

После настройки секретов, деплой будет запускаться автоматически при:
- Пуше в ветки `main`, `master` или `claude/**`
- Изменении файлов в `supabase/functions/**` или `supabase/migrations/**`

### Что деплоится:
1. ✅ **Database migrations** - все миграции из `supabase/migrations/`
2. ✅ **Edge Functions** - все функции из `supabase/functions/`:
   - sync-chats-api
   - sync-questions-api
   - send-chat-message
   - send-chat-file
   - publish-reply
   - и другие...

## 📝 Проверка статуса деплоя

1. Откройте ваш репозиторий на GitHub
2. Перейдите во вкладку `Actions`
3. Увидите историю всех деплоев с их статусами

## 🔧 Тестирование

После настройки секретов можно сделать тестовый коммит:
```bash
echo "# Test autodeploy" >> README.md
git add README.md
git commit -m "Test: trigger autodeploy"
git push
```

Затем проверьте статус в Actions tab на GitHub.

## ❗ Важно

- **Никогда** не коммитьте секреты в код
- Храните пароли только в GitHub Secrets
- Access Token имеет полный доступ к проекту - храните безопасно
- При утечке токена немедленно отзовите его и создайте новый
