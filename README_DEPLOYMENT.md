# 🚀 ИНСТРУКЦИЯ ПО ДЕПЛОЮ - ЧИТАТЬ ПЕРВЫМ

**Дата:** 2026-01-25
**Проблема:** Кнопка "Синхронизация за 14 дней" не появляется после мерджа PR

---

## ✅ ЧТО УЖЕ СДЕЛАНО (ПОДТВЕРЖДЕНО)

Я только что проверил - ВСЁ готово к деплою:

- ✅ **Pull Request #49 смержен** в main (commit d7e1687)
- ✅ **Код кнопки присутствует** в src/pages/Reviews.tsx (строки 317, 318, 919, 924)
- ✅ **Edge Functions обновлены** (sync-ozon v2, auto-generate-drafts v1)
- ✅ **SQL миграция создана** (20260116_setup_new_ozon_sync_logic.sql)
- ✅ **Защита от дублей** реализована на всех уровнях
- ✅ **Документация создана** (7 файлов)

**ВЫВОД:** Код в порядке! Проблема в деплое фронтенда или браузере.

---

## 🎯 ЧТО ДЕЛАТЬ СЕЙЧАС (ПОШАГОВО)

### ШАГ 1: Проверьте Lovable Deployment ⭐ САМОЕ ВАЖНОЕ!

**Проблема скорее всего здесь!** Lovable может не задеплоить фронтенд автоматически.

**Что делать:**

1. Откройте: https://lovable.dev/projects/YOUR_PROJECT_ID
2. Перейдите в **Deployments** (или **Activity**)
3. Найдите последний деплой

**Проверьте:**

| Статус | Что это значит | Что делать |
|--------|----------------|------------|
| ✅ **Success** | Деплой прошел успешно | Перейдите к Шагу 2 (браузер) |
| ⏳ **In Progress** | Деплой еще идет | ПОДОЖДИТЕ 5-10 минут |
| ❌ **Failed** | Ошибка при сборке | Откройте логи, скопируйте ошибку, отправьте мне |
| 🤔 **Нет деплоя** | Lovable не увидел изменения | Нажмите "Redeploy" для ветки main |

**Если деплоя НЕТ в списке:**
```
1. Нажмите кнопку "Redeploy" или "Trigger Deploy"
2. Выберите branch: main
3. Подождите 5-10 минут
```

---

### ШАГ 2: Очистите кеш браузера ⭐ ВАЖНО!

Даже если деплой успешен, браузер может показывать старую версию.

**Вариант А: Принудительная перезагрузка**
```
Windows/Linux: Ctrl + F5
или: Ctrl + Shift + R

Mac: Cmd + Shift + R
```

**Вариант Б: Приватное окно (100% работает)**
```
Chrome: Ctrl+Shift+N (Win) или Cmd+Shift+N (Mac)
Firefox: Ctrl+Shift+P
Safari: Cmd+Shift+N

Затем откройте ваше приложение
```

**Вариант В: Полная очистка кеша**
```
1. F12 (DevTools)
2. Правая кнопка на кнопке "Обновить" в браузере
3. "Empty Cache and Hard Reload"
```

---

### ШАГ 3: Проверьте что кнопка появилась

Откройте: `https://your-app.lovable.app/app/reviews`

**Должны увидеть 4 кнопки:**

```
┌────────────────────────────────────────────────┐
│  [Автогенерация ответов]                      │
│  [Синхронизировать]                            │
│  [Синхронизация за 14 дней]  ← ЭТА НОВАЯ!    │
│  [Обновить]                                    │
└────────────────────────────────────────────────┘
```

**✅ Если кнопка ПОЯВИЛАСЬ:**
- Переходите к Шагу 4 (деплой backend)

**❌ Если кнопки НЕТ:**
- Откройте DevTools (F12) → Console
- Посмотрите на ошибки (красные строки)
- Сделайте скриншот консоли
- Отправьте мне

---

### ШАГ 4: Деплой Edge Functions

Даже если кнопка появилась, нужно задеплоить backend чтобы защита от дублей работала!

```bash
# Перейдите в корень проекта
cd /path/to/standalone-spark

# Войдите в Supabase (если нужно)
supabase login

# Подключитесь к проекту
supabase link --project-ref bkmicyguzlwampuindff

# Деплой sync-ozon (VERSION: 2026-01-16-v2)
supabase functions deploy sync-ozon --no-verify-jwt

# Деплой auto-generate-drafts (VERSION: 2026-01-16-v1)
supabase functions deploy auto-generate-drafts --no-verify-jwt
```

**Вы должны увидеть:**
```
✓ Deployed Function sync-ozon
✓ Deployed Function auto-generate-drafts
```

---

### ШАГ 5: Примените SQL миграцию

Это создаст новые cron jobs для автоматической синхронизации.

**Способ 1: Через SQL Editor (РЕКОМЕНДУЮ)**

1. Откройте: https://supabase.com/dashboard/project/bkmicyguzlwampuindff/sql
2. Откройте файл на своем компьютере: `supabase/migrations/20260116_setup_new_ozon_sync_logic.sql`
3. Скопируйте ВЕСЬ SQL код из файла
4. Вставьте в SQL Editor
5. Нажмите **"Run"**

**Вы должны увидеть:**
```
✓ Success. No rows returned
```

**Способ 2: Через CLI**
```bash
supabase db push
```

---

### ШАГ 6: Проверьте что всё работает

**Проверка 1: Cron jobs созданы**

В Supabase SQL Editor выполните:
```sql
SELECT jobname, schedule, active
FROM cron.job
WHERE jobname LIKE '%ozon%'
ORDER BY jobname;
```

**Должно показать:**
```
sync-ozon-incremental | */10 * * * * | true
sync-ozon-weekly      | 0 3 * * 0    | true
```

**Проверка 2: Синхронизация работает**

```sql
SELECT
  name,
  last_sync_at,
  NOW() - last_sync_at as "время_назад"
FROM marketplaces
WHERE type = 'ozon';
```

**Должно показать:**
- `время_назад` меньше 15 минут

**Проверка 3: Дублей нет**

```sql
SELECT
  r.id as review_id,
  COUNT(rep.id) as replies_count
FROM reviews r
LEFT JOIN replies rep ON rep.review_id = r.id AND rep.deleted_at IS NULL
WHERE r.deleted_at IS NULL
GROUP BY r.id
HAVING COUNT(rep.id) > 1;
```

**Должно показать:**
- **ПУСТО** (0 строк) = дублей нет! ✅

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема 1: Кнопка не появляется

**Откройте файл:** `ЧЕКЛИСТ_ФРОНТЕНД.md`
Там пошаговая диагностика для фронтенда.

**Быстрая диагностика в браузере:**
```javascript
// Откройте DevTools (F12) → Console, вставьте:
fetch('/assets/index.js').then(r => r.text()).then(code => {
  if (code.includes('Синхронизация за 14 дней')) {
    console.log('✅ Код новой кнопки найден в бандле')
  } else {
    console.log('❌ Код кнопки НЕ найден - Lovable не задеплоил!')
  }
})
```

---

### Проблема 2: Синхронизация не работает

**Откройте файл:** `ДИАГНОСТИКА_ПРОБЛЕМЫ.sql`
Там 8 SQL запросов для полной диагностики backend.

**Проверьте логи Edge Functions:**
1. Откройте: https://supabase.com/dashboard/project/bkmicyguzlwampuindff/logs/edge-functions
2. Найдите в логах `sync-ozon`
3. Должны увидеть: `[sync-ozon] Found N reviews with published replies`
4. Если этого НЕТ - функции не задеплоены или старая версия!

---

### Проблема 3: Дубли продолжают появляться

**Причина:** Edge Functions не задеплоены или работают старые версии.

**Решение:**
```bash
# Переделайте деплой
supabase functions deploy sync-ozon --no-verify-jwt
supabase functions deploy auto-generate-drafts --no-verify-jwt

# Проверьте логи (должны увидеть новые версии)
```

---

## 📁 ФАЙЛЫ ДЛЯ ДИАГНОСТИКИ

У вас есть следующие файлы для помощи:

| Файл | Для чего |
|------|----------|
| **README_DEPLOYMENT.md** | ⭐ Этот файл - читайте первым |
| **ЧЕКЛИСТ_ФРОНТЕНД.md** | Диагностика проблем с кнопкой |
| **ДИАГНОСТИКА_ПРОБЛЕМЫ.sql** | SQL запросы для проверки backend |
| **БЫСТРОЕ_РЕШЕНИЕ.md** | Краткое решение проблемы |
| **ДЕПЛОЙ_ВЕРСИИ.md** | Команды для деплоя функций |
| **scripts/verify_deployment.sh** | Скрипт автоматической проверки (Linux/Mac) |
| **scripts/verify_deployment.ps1** | Скрипт автоматической проверки (Windows) |

**Запуск скрипта проверки:**
```bash
# Linux/macOS
./scripts/verify_deployment.sh

# Windows
.\scripts\verify_deployment.ps1
```

---

## 📊 ТЕКУЩИЙ СТАТУС (ПРОВЕРЕНО СКРИПТОМ)

```
✅ Pull Request #49 смержен в main
✅ Код кнопки в src/pages/Reviews.tsx (строки 317, 318, 919, 924)
✅ sync-ozon VERSION: 2026-01-16-v2
✅ auto-generate-drafts VERSION: 2026-01-16-v1
✅ Защита от дублей присутствует в обеих функциях
✅ SQL миграция создана
✅ Документация создана
```

**ПРОБЛЕМА:** Скорее всего Lovable не задеплоил фронтенд автоматически после мерджа.

**РЕШЕНИЕ:** Шаги 1-2 выше (Lovable Redeploy + очистка кеша браузера).

---

## 🎯 TL;DR (ОЧЕНЬ КРАТКО)

1. **Проверьте Lovable** - есть ли успешный деплой? Если нет - нажмите "Redeploy"
2. **Очистите кеш** - Ctrl+F5 или приватное окно
3. **Откройте /app/reviews** - должна появиться кнопка "Синхронизация за 14 дней"
4. **Задеплойте функции:**
   ```bash
   supabase functions deploy sync-ozon --no-verify-jwt
   supabase functions deploy auto-generate-drafts --no-verify-jwt
   ```
5. **Примените SQL миграцию** в Supabase SQL Editor
6. **Проверьте что работает** (SQL запросы выше)

---

## ✅ КОГДА ВСЁ ЗАРАБОТАЕТ

Вы увидите:

**UI:**
- ✅ Кнопка "Синхронизация за 14 дней" на странице Отзывы
- ✅ Обе кнопки синхронизации работают

**Backend:**
- ✅ Синхронизация каждые 10 минут (автоматически)
- ✅ Полная синхронизация раз в неделю (автоматически)
- ✅ Дублей НЕТ (проверено SQL запросом)
- ✅ Защита от повторных публикаций работает

**Функции:**
- ✅ sync-ozon версия 2026-01-16-v2
- ✅ auto-generate-drafts версия 2026-01-16-v1

---

## 💬 СВЯЖИТЕСЬ СО МНОЙ

Если после выполнения шагов выше что-то не работает:

**Отправьте мне:**
1. Screenshot страницы "Отзывы"
2. Screenshot Lovable Deployment Status
3. Screenshot браузерной консоли (F12 → Console)
4. Результаты SQL запросов из Шага 6

Я помогу решить проблему! 🚀

---

**Дата создания:** 2026-01-25
**Версия:** 1.0
**Автор:** Claude Code Agent
