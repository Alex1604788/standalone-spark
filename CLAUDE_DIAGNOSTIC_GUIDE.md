# 🤖 Руководство по диагностике для Claude

## Обзор

Этот документ описывает инструменты диагностики для самостоятельной проверки и тестирования проекта standalone-spark.

## 🔧 Доступные инструменты

### 1. **Быстрая проверка системы**
```bash
npm run check
# или
./check-system.sh
```

**Что проверяет:**
- ✅ Node.js и npm версии
- ✅ Наличие зависимостей
- ✅ Структура проекта
- ✅ Конфигурационные файлы
- ✅ Unit тесты
- ✅ Git статус

**Когда использовать:**
- При начале работы
- После git pull
- Для быстрой проверки окружения

---

### 2. **Unit тесты логики дедупликации**
```bash
npm test
# или
npm run test:dedup
# или
node test-deduplication-logic.test.js
```

**Что тестирует:**
- ✅ Логика дедупликации расходов из `PromotionAnalytics.tsx:268-282`
- ✅ Обработка дублирующихся записей
- ✅ Группировка по кампаниям
- ✅ Обработка NULL значений
- ✅ Выбор MAX значения при расхождениях

**6 тестовых сценариев:**
1. Нет дублирования
2. Простое дублирование (2 товара)
3. Реальный сценарий (5 товаров, 7 дней) - проверка на ~24 428 ₽
4. Несколько кампаний
5. Записи без campaign_id
6. Разные значения в один день

**Exit code:**
- 0 = все тесты пройдены ✅
- 1 = есть провалившиеся тесты ❌

---

### 3. **Полная диагностическая система** (требует сетевого доступа)
```bash
npm run diagnostic
# или
node diagnostic-suite.mjs
```

**Что проверяет:**
- 🔌 Подключение к Supabase
- 📊 Структура данных (таблицы и VIEW)
- 📦 Данные кампании "Кабель КГ 2*2,5"
- ✅ Валидация дедупликации на реальных данных
- 🔄 Актуальность синхронизации OZON
- 📈 Генерация отчета с оценкой здоровья системы

**Примечание:** Требует доступ к интернету и Supabase API.

---

## 📁 Структура файлов диагностики

```
standalone-spark/
├── check-system.sh                    # Быстрая проверка окружения
├── test-deduplication-logic.test.js   # Unit тесты без БД
├── diagnostic-suite.mjs               # Полная диагностика с БД
├── run-diagnostics.mjs                # Диагностика расходов (старый)
├── run-diagnostics.py                 # Диагностика расходов (Python)
├── test-dedup-logic.py                # Тест дедупликации (Python)
├── debug-campaign-expenses.sql        # SQL запросы для ручной диагностики
└── ДИАГНОСТИКА_РАСХОДОВ.md            # Документация проблемы
```

---

## 🎯 Типичные сценарии использования

### Сценарий 1: Начало работы
```bash
# 1. Проверить что все работает
npm run check

# 2. Запустить unit тесты
npm test

# 3. Если все ✅, можно начинать работу
```

### Сценарий 2: Проверка исправления дедупликации
```bash
# 1. Запустить unit тесты
npm run test:dedup

# 2. Проверить результаты:
#    - Тест 3 должен показывать ~24 428 ₽ за неделю
#    - Экономия должна быть ~80%
```

### Сценарий 3: Проверка данных в production
```bash
# Если есть сетевой доступ
npm run diagnostic

# Альтернатива: SQL запросы вручную через Supabase Dashboard
# Файл: debug-campaign-expenses.sql
```

### Сценарий 4: После git pull
```bash
# Быстрая проверка что ничего не сломалось
npm run check && npm test
```

---

## 🔍 Понимание проблемы с дублированием

### Суть проблемы

OZON Performance API возвращает `money_spent` **на уровне кампании за день**.

**Пример данных в БД:**
```
stat_date   | campaign_id | sku   | money_spent
------------|-------------|-------|------------
2025-12-01  | 11033377    | SKU-1 | 1000
2025-12-01  | 11033377    | SKU-2 | 1000  <- Тот же money_spent!
2025-12-01  | 11033377    | SKU-3 | 1000  <- И снова!
```

### Что было до исправления
```javascript
// НЕПРАВИЛЬНО
const total = data.reduce((sum, row) => sum + row.money_spent, 0);
// 1000 + 1000 + 1000 = 3000 ❌ (дублирование в 3 раза!)
```

### Что сейчас (исправленная логика)
```javascript
// ПРАВИЛЬНО: дедупликация по (campaign_id, stat_date)
const campaignDailyExpenses = new Map<string, Map<string, number>>();
performanceData.forEach(row => {
  const campaignId = String(row.campaign_id);
  const date = row.stat_date;

  if (!campaignDailyExpenses.has(campaignId)) {
    campaignDailyExpenses.set(campaignId, new Map());
  }

  const dailyMap = campaignDailyExpenses.get(campaignId);
  dailyMap.set(date, Math.max(dailyMap.get(date) || 0, Number(row.money_spent)));
});

// Результат: 1000 ✅ (правильно!)
```

---

## 📊 Ожидаемые значения

### Кампания "Кабель КГ 2*2,5"

| Период | Ожидаемая сумма | Источник |
|--------|-----------------|----------|
| 1-7 декабря | ~24 428 ₽ | OZON Seller |
| 1-31 декабря | ~109 130 ₽ | OZON Seller |

### Если числа не совпадают

1. **Меньше ожидаемого** → проверить что все дни загружены в БД
2. **Больше ожидаемого** → проблема с дедупликацией
3. **За больший период меньше** → данные неполные

---

## 🚨 Troubleshooting

### `npm run diagnostic` не работает (fetch failed)
**Причина:** Нет сетевого доступа к Supabase API

**Решение:**
1. Использовать `npm test` для локальных тестов
2. Выполнить SQL запросы вручную через Supabase Dashboard
3. Запустить Python версию: `python3 run-diagnostics.py`

### Тесты проваливаются
**Причина:** Логика дедупликации изменилась

**Действия:**
1. Проверить `src/pages/analytics/PromotionAnalytics.tsx:268-282`
2. Обновить тесты если логика изменилась намеренно
3. Исправить логику если тесты правильные

### Git hook блокирует commit
**Причина:** Есть незакоммиченные файлы

**Решение:**
```bash
git status
git add <files>
git commit -m "message"
git push
```

---

## 📝 Логи и консоль

### Что смотреть в консоли браузера

В `PromotionAnalytics.tsx` есть консольный вывод:
```javascript
console.log("💰 Дедуплицированные расходы по кампаниям:");
campaignDailyExpenses.forEach((dailyMap, campaignId) => {
  const campaign = campaignMap.get(campaignId);
  if (campaign) {
    console.log(`  - ${campaign.campaign_name}:`, {
      days: dailyMap.size,
      dailyExpenses: uniqueDailyExpenses.slice(0, 5),
      total: campaign.total_money_spent
    });
  }
});
```

**Что проверять:**
- `days` должно быть = количеству дней в периоде
- `total` должен совпадать с OZON Seller

---

## 🎓 Для разработчиков

### Добавление новых тестов

Добавьте тест в `test-deduplication-logic.test.js`:

```javascript
console.log(`\n${colors.cyan}Тест N: Описание${colors.reset}`);
const testData = [
  // ... данные
];
const result = deduplicateExpenses(testData);
assertEquals(result.total, EXPECTED_VALUE, "Описание проверки");
```

### Структура теста

```javascript
function assertEquals(actual, expected, testName) {
  testsRun++;
  if (actual === expected) {
    testsPassed++;
    console.log(`✓ ${testName}`);
  } else {
    testsFailed++;
    console.log(`✗ ${testName}`);
    console.log(`  Expected: ${expected}, Got: ${actual}`);
  }
}
```

---

## 📚 Связанные документы

- `ДИАГНОСТИКА_РАСХОДОВ.md` - Описание проблемы с расходами
- `debug-campaign-expenses.sql` - SQL запросы для диагностики
- `src/pages/analytics/PromotionAnalytics.tsx` - Основная логика (строки 257-410)

---

## ✅ Checklist для Claude

Перед началом работы:
- [ ] `npm run check` прошел успешно
- [ ] `npm test` все тесты ✅
- [ ] Git ветка правильная
- [ ] Нет незакоммиченных изменений (или знаю о них)

Перед commit/push:
- [ ] `npm test` все тесты ✅
- [ ] Код отформатирован
- [ ] Нет console.log (кроме диагностических)
- [ ] Commit message информативный

После исправления багов:
- [ ] Добавлены тесты для нового кейса
- [ ] Все существующие тесты ✅
- [ ] Документация обновлена если нужно

---

**Последнее обновление:** 2026-01-15
**Версия:** 1.0.0
