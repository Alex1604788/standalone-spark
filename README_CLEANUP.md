# 🧹 Система анализа и очистки таблицы REPLIES

Автоматическая система для анализа и очистки старых записей из таблицы `replies`.

## 📁 Файлы проекта

### Анализ

- **analyze-final.sh** - Полный анализ таблицы REPLIES
  - Показывает статистику по всем записям
  - Анализирует распределение по статусам и периодам
  - Вычисляет потенциал очистки
  - Использует: bash + curl + jq

### Очистка

- **CREATE_CLEANUP_FUNCTION.sql** - PostgreSQL функция автоочистки
  - Создает функцию `cleanup_old_replies()`
  - Удаляет записи старше 30 дней
  - Возвращает JSON с результатами
  - Готова для pg_cron

- **cleanup-replies.sh** - Bash скрипт разовой очистки
  - Работает через REST API
  - Показывает превью перед удалением
  - Требует подтверждение (YES)

### Документация

- **CLEANUP_SETUP.md** - Полная инструкция по настройке
  - Настройка pg_cron
  - Настройка локального cron
  - Примеры и решение проблем

### Тестирование

- **TEST_CLEANUP.sql** - SQL тест системы очистки
  - Создает тестовые записи
  - Запускает очистку
  - Проверяет результат

### Служебные

- **CREATE_ANALYSIS_FUNCTION.sql** - PostgreSQL функция анализа
  - Создает функцию `analyze_replies_table()`
  - Альтернатива bash скрипту (не используется)

- **run-auto-analysis.mjs** - Node.js скрипт анализа через RPC
  - Вызывает `analyze_replies_table()`
  - Проблемы с сетью в контейнере (не используется)

- **setup-auto-analysis.mjs** - Helper для установки анализа
  - Инструкции по установке (не используется)

---

## 🚀 Быстрый старт

### 1. Анализ таблицы

```bash
./analyze-final.sh
```

Покажет:
- Общую статистику
- Распределение по статусам
- Старые записи
- Потенциал очистки

### 2. Разовая очистка

```bash
./cleanup-replies.sh
# Введите YES для подтверждения
```

### 3. Автоматическая очистка (pg_cron)

1. Выполните в Supabase SQL Editor: `CREATE_CLEANUP_FUNCTION.sql`
2. Настройте автозапуск:

```sql
SELECT cron.schedule(
    'cleanup-old-replies-daily',
    '0 2 * * *',
    $$ SELECT public.cleanup_old_replies(); $$
);
```

Подробнее в: **CLEANUP_SETUP.md**

---

## 📊 Стратегия очистки

**Агрессивная:** Все записи старше 30 дней удаляются

| Статус    | Правило               | Приоритет |
|-----------|-----------------------|-----------|
| failed    | Удалить > 30 дней     | Высокий   |
| drafted   | Удалить > 30 дней     | Высокий   |
| published | Удалить > 30 дней     | Высокий   |

**Логика:** Ответы на отзывы не имеют ценности после публикации. 30 дней достаточно для хранения истории.

---

## 🧪 Тестирование

### Вариант 1: SQL тест (рекомендуется)

1. Выполните `CREATE_CLEANUP_FUNCTION.sql` в Supabase SQL Editor
2. Выполните `TEST_CLEANUP.sql` в Supabase SQL Editor
3. Проверьте результат: должно быть "✅ ТЕСТ ПРОЙДЕН"

### Вариант 2: Bash тест

```bash
# Создайте тестовые данные в Supabase (через SQL Editor)
# Затем запустите:
./cleanup-replies.sh
```

---

## 📈 Мониторинг

### Проверить состояние таблицы

```bash
./analyze-final.sh
```

### Посмотреть историю очистки (pg_cron)

```sql
SELECT * FROM cron.job_run_details
WHERE job_name = 'cleanup-old-replies-daily'
ORDER BY start_time DESC LIMIT 5;
```

---

## ⚙️ Требования

- **bash** - для скриптов
- **curl** - для REST API запросов
- **jq** - для парсинга JSON
- **PostgreSQL** - для функций
- **pg_cron** (опционально) - для автоматической очистки

---

## 📝 Changelog

### 2026-01-25
- ✅ Создана система анализа (analyze-final.sh)
- ✅ Создана система очистки (cleanup-replies.sh)
- ✅ Создана PostgreSQL функция (cleanup_old_replies)
- ✅ Добавлена документация (CLEANUP_SETUP.md)
- ✅ Добавлены SQL тесты (TEST_CLEANUP.sql)

---

## 🆘 Поддержка

При проблемах смотрите:
- **CLEANUP_SETUP.md** - решение проблем
- Логи pg_cron: `cron.job_run_details`
- Проверьте права доступа в Supabase

---

**Автор:** Claude (AI Assistant)
**Дата:** 2026-01-25
**Версия:** 1.0
