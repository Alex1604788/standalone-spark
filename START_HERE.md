# 🚀 НАЧНИТЕ ЗДЕСЬ - Автоматическая настройка системы

**Проект:** КРАФТМАН (standalone-spark)
**Дата:** 2026-02-20
**Время:** ~15 минут

---

## ⚡ ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС (3 ПРОСТЫХ ШАГА)

Я создал автоматические скрипты для полной настройки системы. Следуйте этой инструкции:

---

### ШАГ 1️⃣: Настройка базы данных (2 минуты)

1. Откройте Supabase SQL Editor:
   👉 https://supabase.com/dashboard/project/bkmicyguzlwampuindff/sql/new

2. Откройте файл `AUTO_SETUP_SYSTEM.sql` (в корне проекта)

3. Скопируйте **ВЕСЬ** файл (Ctrl+A → Ctrl+C)

4. Вставьте в SQL Editor (Ctrl+V)

5. Нажмите **Run** (или F5)

6. Дождитесь сообщения: `✅ АВТОМАТИЧЕСКАЯ НАСТРОЙКА ЗАВЕРШЕНА!`

**Что произойдет автоматически:**
- ✅ Удалятся старые cron jobs
- ✅ Создадутся новые cron jobs для синхронизации (каждые 10 минут + раз в неделю)
- ✅ Удалятся дубли ответов (168 отзывов)
- ✅ Создастся резервная копия данных

---

### ШАГ 2️⃣: Деплой Edge Functions (5-10 минут)

Откройте терминал в корне проекта и выполните:

```bash
cd /home/user/standalone-spark
./AUTO_DEPLOY.sh
```

**Что произойдет автоматически:**
- ✅ Задеплоятся все Edge Functions:
  - sync-ozon (синхронизация отзывов)
  - sync-chats-api (синхронизация чатов)
  - auto-generate-drafts (автогенерация ответов)
  - process-scheduled-replies (обработка запланированных ответов)
  - publish-reply (публикация ответов)
  - И другие функции

**Если скрипт выдает ошибку:**
```bash
# Установите Supabase CLI (выберите способ для вашей ОС)

# Linux/macOS:
curl -fsSL https://supabase.com/install.sh | sh

# Или через Homebrew (macOS):
brew install supabase/tap/supabase

# После установки повторите:
./AUTO_DEPLOY.sh
```

---

### ШАГ 3️⃣: Проверка работы (2 минуты)

Выполните в Supabase SQL Editor:

```sql
-- Проверить что cron jobs созданы
SELECT jobname, schedule, active
FROM cron.job
WHERE jobname LIKE '%ozon%'
ORDER BY jobname;
```

**Ожидаемый результат:**
```
sync-ozon-incremental | */10 * * * * | true   ← Каждые 10 минут
sync-ozon-weekly      | 0 3 * * 0    | true   ← Раз в неделю
```

Если вы видите эти две строки - **ВСЁ НАСТРОЕНО ПРАВИЛЬНО!** ✅

---

## 🎉 ГОТОВО! ЧТО ДАЛЬШЕ?

### Следующие 30 минут:

1. **Подождите 10-15 минут** - первый cron job должен запуститься автоматически

2. **Проверьте синхронизацию:**
   ```sql
   SELECT name, last_sync_at, NOW() - last_sync_at as time_since_sync
   FROM marketplaces WHERE type = 'ozon';
   ```
   Должно быть: `time_since_sync` < 15 минут

3. **Проверьте логи Edge Functions:**
   👉 https://supabase.com/dashboard/project/bkmicyguzlwampuindff/logs/edge-functions

   Должны появиться записи о синхронизации

### Опционально: Ручной запуск синхронизации

Если хотите сразу проверить что всё работает:

1. Откройте приложение: **Отзывы** → **Синхронизация**
2. Нажмите: **"Синхронизация за 14 дней"**
3. Дождитесь завершения (5-10 минут)

---

## 📊 ПРОВЕРКА РАЗМЕРА БД (ВАЖНО!)

После настройки проверьте размер базы данных:

```bash
# Откройте SQL Editor
# Скопируйте и выполните файл: CHECK_DATABASE_SIZE.sql
```

**Если размер > 400 МБ:**

Выполните очистку старых данных:

```sql
-- Удалить отзывы старше 180 дней
SELECT public.cleanup_old_reviews(180);

-- Освободить место
VACUUM ANALYZE reviews;
VACUUM ANALYZE replies;
```

Подробнее см. файл: `DATABASE_SIZE_OPTIMIZATION.md`

---

## 📁 СОЗДАННЫЕ ФАЙЛЫ

Я создал для вас:

### Автоматическая настройка:
- ✅ `AUTO_SETUP_SYSTEM.sql` - SQL для настройки БД
- ✅ `AUTO_DEPLOY.sh` - Bash скрипт для деплоя функций
- ✅ `AUTO_SETUP_GUIDE.md` - Подробная инструкция (с решением проблем)

### Оптимизация базы данных:
- ✅ `CHECK_DATABASE_SIZE.sql` - Проверка размера БД
- ✅ `DATABASE_SIZE_OPTIMIZATION.md` - Инструкция по оптимизации
- ✅ `DELETE_OLD_REVIEWS_MANUAL.sql` - Ручное удаление старых данных
- ✅ `supabase/migrations/20260220_create_cleanup_old_reviews_function.sql` - Функция автоочистки

### Справочные:
- ✅ `DIAGNOSTIC_REPORT_2026-02-20.md` - Отчет о проблемах
- ✅ `CLEANUP_SETUP.md` - Настройка автоочистки replies

---

## ⚠️ ЧТО БЫЛО ИСПРАВЛЕНО

Система автоматически исправит критические проблемы:

1. **Синхронизация остановлена 42 дня**
   - ✅ Создаются новые cron jobs
   - ✅ Синхронизация каждые 10 минут
   - ✅ Полная синхронизация раз в неделю

2. **168 отзывов с дублями ответов**
   - ✅ Автоматическая очистка дублей
   - ✅ Резервная копия перед удалением

3. **База данных приближается к лимиту 500 МБ**
   - ✅ Функция для очистки старых отзывов
   - ✅ Автоматическая очистка replies старше 30 дней
   - ✅ Инструкции по оптимизации

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НE ТАК

### Проблема: Cron jobs не создались

**Решение:**
1. Убедитесь что pg_cron включен:
   ```sql
   CREATE EXTENSION IF NOT EXISTS pg_cron;
   ```
2. Повторно выполните `AUTO_SETUP_SYSTEM.sql`

---

### Проблема: Edge Functions не деплоятся

**Решение:**
1. Проверьте что Supabase CLI установлен:
   ```bash
   supabase --version
   ```
2. Если нет - установите (см. ШАГ 2)
3. Залинкуйте проект:
   ```bash
   supabase link --project-ref bkmicyguzlwampuindff
   ```
4. Повторите деплой:
   ```bash
   ./AUTO_DEPLOY.sh
   ```

---

### Проблема: Синхронизация не запускается

**Проверьте:**

1. **Cron jobs активны:**
   ```sql
   SELECT * FROM cron.job WHERE jobname LIKE '%ozon%';
   ```

2. **API ключи заполнены:**
   ```sql
   SELECT name, service_account_email, api_key_encrypted
   FROM marketplaces WHERE type = 'ozon';
   ```

3. **Логи cron jobs:**
   ```sql
   SELECT * FROM cron.job_run_details
   ORDER BY start_time DESC LIMIT 10;
   ```

---

## 📖 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

- **Полная инструкция:** `AUTO_SETUP_GUIDE.md`
- **Решение проблем:** `DIAGNOSTIC_REPORT_2026-02-20.md`
- **Оптимизация БД:** `DATABASE_SIZE_OPTIMIZATION.md`
- **Настройка автоочистки:** `CLEANUP_SETUP.md`

---

## ✅ ЧЕКЛИСТ ВЫПОЛНЕНИЯ

Отметьте что вы сделали:

- [ ] ШАГ 1: Выполнен `AUTO_SETUP_SYSTEM.sql` в Supabase
- [ ] ШАГ 2: Запущен `./AUTO_DEPLOY.sh`
- [ ] ШАГ 3: Проверены cron jobs (2 задачи активны)
- [ ] Проверен размер БД (< 400 МБ желательно)
- [ ] Настроена автоочистка старых данных
- [ ] Проверены логи Edge Functions (нет ошибок)
- [ ] Запущена ручная синхронизация (опционально)

**Если всё отмечено - система настроена и готова к работе!** 🎉

---

**Дата создания:** 2026-02-20
**Автор:** Claude (AI Assistant)
**Версия:** 1.0

**Следующая проверка:** Через 24 часа убедитесь что синхронизация работает автоматически
