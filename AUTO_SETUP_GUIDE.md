# 🚀 АВТОМАТИЧЕСКАЯ НАСТРОЙКА СИСТЕМЫ OZON

**Проект:** КРАФТМАН (standalone-spark)
**Дата:** 2026-02-20
**Время выполнения:** ~15 минут

---

## 📋 Что будет сделано автоматически

1. ✅ Удаление старых cron jobs
2. ✅ Создание новых cron jobs для синхронизации
3. ✅ Очистка дублей ответов в БД
4. ✅ Деплой всех Edge Functions
5. ✅ Проверка работоспособности

---

## 🎯 БЫСТРЫЙ СТАРТ (3 ШАГА)

### ШАГ 1: Настройка БД (SQL)

**Время:** 2 минуты

1. Откройте [Supabase SQL Editor](https://supabase.com/dashboard/project/bkmicyguzlwampuindff/sql/new)

2. Скопируйте **ВЕСЬ** файл `AUTO_SETUP_SYSTEM.sql` (в корне проекта)

3. Вставьте в SQL Editor и нажмите **Run** (F5)

4. Дождитесь завершения (увидите сообщение "✅ АВТОМАТИЧЕСКАЯ НАСТРОЙКА ЗАВЕРШЕНА!")

**Что произойдет:**
- Удалятся старые cron jobs
- Создадутся новые:
  - `sync-ozon-incremental` - каждые 10 минут
  - `sync-ozon-weekly` - раз в неделю
- Удалятся дубли ответов (создастся backup)
- Появится финальный отчет

---

### ШАГ 2: Деплой Edge Functions

**Время:** 5-10 минут

```bash
cd /home/user/standalone-spark
./AUTO_DEPLOY.sh
```

**Что произойдет:**
- Задеплоятся все критические функции:
  - sync-ozon
  - sync-chats-api
  - auto-generate-drafts
  - auto-generate-drafts-cron
  - process-scheduled-replies
  - publish-reply
- И дополнительные функции
- Появится отчет о результатах

**Если ошибка "supabase CLI не установлен":**
```bash
npm install -g supabase
```

---

### ШАГ 3: Проверка работы

**Время:** 2 минуты

1. **Проверьте cron jobs:**

Выполните в [SQL Editor](https://supabase.com/dashboard/project/bkmicyguzlwampuindff/sql/new):

```sql
SELECT
  jobname,
  schedule,
  active,
  jobid
FROM cron.job
WHERE jobname LIKE '%ozon%'
ORDER BY jobname;
```

**Ожидаемый результат:**
```
sync-ozon-incremental | */10 * * * * | true
sync-ozon-weekly      | 0 3 * * 0    | true
```

2. **Проверьте Edge Functions:**

Откройте [Edge Functions Logs](https://supabase.com/dashboard/project/bkmicyguzlwampuindff/logs/edge-functions)

**Что искать:**
- Должны появиться новые запуски функций в течение 10-15 минут
- Не должно быть ERROR сообщений

3. **Запустите синхронизацию вручную (опционально):**

В приложении:
- Перейдите: Отзывы → Кнопка "Синхронизация"
- Нажмите "Синхронизация за 14 дней"
- Дождитесь завершения (5-10 минут)

---

## ✅ КАК ПРОВЕРИТЬ ЧТО ВСЕ РАБОТАЕТ

### 1. Cron Jobs работают

```sql
-- Проверить последние запуски cron jobs
SELECT
  job_run_details.job_id,
  cron.job.jobname,
  job_run_details.status,
  job_run_details.start_time,
  job_run_details.end_time,
  job_run_details.return_message
FROM cron.job_run_details
JOIN cron.job ON job_run_details.job_id = cron.job.jobid
WHERE cron.job.jobname LIKE '%ozon%'
ORDER BY job_run_details.start_time DESC
LIMIT 10;
```

**Ожидается:** Запуски каждые 10 минут, status = 'succeeded'

---

### 2. Синхронизация работает

```sql
-- Проверить последнюю синхронизацию
SELECT
  name,
  last_sync_at,
  NOW() - last_sync_at as time_since_sync
FROM marketplaces
WHERE type = 'ozon';
```

**Ожидается:** last_sync_at < 15 минут назад

---

### 3. Дубли удалены

```sql
-- Проверить наличие дублей
SELECT
  review_id,
  COUNT(*) as count
FROM replies
WHERE deleted_at IS NULL
  AND status IN ('drafted', 'scheduled')
  AND review_id IS NOT NULL
GROUP BY review_id
HAVING COUNT(*) > 1
ORDER BY count DESC
LIMIT 10;
```

**Ожидается:** Пустой результат (0 строк)

---

### 4. Edge Functions работают

Откройте [Edge Functions Logs](https://supabase.com/dashboard/project/bkmicyguzlwampuindff/logs/edge-functions)

**Фильтры:**
- Function: sync-ozon
- Уровень: Info, Error

**Ожидается:**
- ✅ Запуски каждые 10 минут
- ✅ Сообщения "[OZON SYNC] Starting sync..."
- ✅ Сообщения "[OZON SYNC] Sync completed"
- ❌ НЕТ ERROR сообщений

---

## 🔧 РАСШИРЕННАЯ ДИАГНОСТИКА

Если что-то не работает, запустите полную диагностику:

```bash
cd /home/user/standalone-spark
./full-diagnostics.sh
```

Это создаст подробный отчет с:
- Статусом cron jobs
- Статистикой синхронизации
- Наличием дублей
- Информацией о маркетплейсах
- Рекомендациями по исправлению

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: Cron jobs не создаются

**Симптом:**
```sql
SELECT * FROM cron.job WHERE jobname LIKE '%ozon%';
-- Пустой результат
```

**Решение:**
1. Убедитесь что расширение pg_cron включено:
   ```sql
   CREATE EXTENSION IF NOT EXISTS pg_cron;
   ```
2. Повторно запустите `AUTO_SETUP_SYSTEM.sql`
3. Если не помогло - проверьте права доступа (нужен SUPERUSER или роль с правами на cron)

---

### Проблема 2: Edge Functions не деплоятся

**Симптом:**
```
Error: Failed to deploy function
```

**Решение:**
1. Проверьте подключение к интернету
2. Проверьте что проект залинкован:
   ```bash
   supabase link --project-ref bkmicyguzlwampuindff
   ```
3. Попробуйте задеплоить функцию вручную:
   ```bash
   supabase functions deploy sync-ozon --no-verify-jwt
   ```

---

### Проблема 3: Синхронизация не запускается

**Симптом:**
- last_sync_at не обновляется
- В логах Edge Functions нет новых записей

**Проверьте:**

1. **API ключи заполнены:**
   ```sql
   SELECT
     name,
     service_account_email IS NOT NULL as has_client_id,
     api_key_encrypted IS NOT NULL as has_api_key
   FROM marketplaces
   WHERE type = 'ozon';
   ```
   Оба поля должны быть `true`

2. **Cron jobs активны:**
   ```sql
   SELECT jobname, active FROM cron.job WHERE jobname LIKE '%ozon%';
   ```
   Поле `active` должно быть `true`

3. **Логи cron jobs:**
   ```sql
   SELECT * FROM cron.job_run_details
   WHERE job_id IN (SELECT jobid FROM cron.job WHERE jobname LIKE '%ozon%')
   ORDER BY start_time DESC
   LIMIT 5;
   ```
   Проверьте поле `return_message` на наличие ошибок

---

### Проблема 4: Дубли не удаляются

**Симптом:**
После выполнения `AUTO_SETUP_SYSTEM.sql` дубли остались

**Решение:**
1. Проверьте backup:
   ```sql
   SELECT COUNT(*) FROM replies_backup_20260220;
   ```
   Если пусто - backup не создался, есть проблема с правами

2. Выполните очистку вручную:
   ```sql
   WITH duplicates AS (
     SELECT
       id,
       ROW_NUMBER() OVER (PARTITION BY review_id ORDER BY created_at ASC) as rn
     FROM replies
     WHERE review_id IS NOT NULL
       AND deleted_at IS NULL
       AND status IN ('drafted', 'scheduled')
   )
   UPDATE replies
   SET deleted_at = NOW()
   WHERE id IN (SELECT id FROM duplicates WHERE rn > 1);
   ```

---

## 📊 МОНИТОРИНГ ПОСЛЕ НАСТРОЙКИ

Рекомендуется проверять систему **первые 3 дня** каждые несколько часов:

### День 1 (сегодня)
- ✅ Через 10 минут: проверить первый запуск cron job
- ✅ Через 30 минут: проверить что синхронизация работает
- ✅ Через 1 час: проверить что новые отзывы появляются

### День 2-3
- ✅ Утром: проверить что синхронизация работала всю ночь
- ✅ Вечером: проверить количество новых отзывов

### После 3 дней
- ✅ Раз в неделю: проверить статус системы через `full-diagnostics.sh`

---

## 📁 СОЗДАННЫЕ ФАЙЛЫ

После выполнения автонастройки в проекте появятся:

| Файл | Описание |
|------|----------|
| `AUTO_SETUP_SYSTEM.sql` | SQL скрипт для настройки БД |
| `AUTO_DEPLOY.sh` | Bash скрипт для деплоя функций |
| `AUTO_SETUP_GUIDE.md` | Эта инструкция |
| `replies_backup_20260220` | Резервная копия ответов (в БД) |

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ ПОСЛЕ НАСТРОЙКИ

1. **Проверьте размер базы данных:**
   ```bash
   # Выполните в SQL Editor
   # См. файл: CHECK_DATABASE_SIZE.sql
   ```

2. **Настройте автоочистку старых данных:**
   ```bash
   # См. инструкцию: DATABASE_SIZE_OPTIMIZATION.md
   ```

3. **Мониторьте работу системы:**
   - Edge Functions Logs
   - Cron job статус
   - Размер БД

---

## 🆘 НУЖНА ПОМОЩЬ?

1. Запустите диагностику:
   ```bash
   ./full-diagnostics.sh
   ```

2. Проверьте файлы:
   - `DIAGNOSTIC_REPORT_2026-02-20.md` - текущий отчет
   - `ДИАГНОСТИКА_ПРОБЛЕМЫ.sql` - SQL запросы для проверки

3. Проверьте логи:
   - [Edge Functions](https://supabase.com/dashboard/project/bkmicyguzlwampuindff/logs/edge-functions)
   - [Database](https://supabase.com/dashboard/project/bkmicyguzlwampuindff/logs/postgres-logs)

---

**Дата создания:** 2026-02-20
**Автор:** Claude (AI Assistant)
**Версия:** 1.0
