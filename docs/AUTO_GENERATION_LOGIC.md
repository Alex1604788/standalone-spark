# 📋 Логика автоматической генерации ответов

## 🎯 Как это работает

### 1. Когда запускается генерация?

**Автоматически:**
- При открытии страницы "Отзывы" с фильтром "Не отвечено" (только на первой странице)
- При нажатии кнопки **"Автогенерация ответов"** в разделе "Отзывы и вопросы"

**Вручную:**
- Кнопка "Автогенерация ответов" в правом верхнем углу страницы "Отзывы"

### 2. Сколько отзывов обрабатывается за раз?

- **Лимит:** до **50 отзывов** за один запуск функции `auto-generate-drafts`
- Обрабатываются только отзывы со статусом **"Не отвечено"** (segment = 'unanswered')
- Для каждого маркетплейса запускается отдельный вызов функции

### 3. Скорость обработки

#### С шаблонами (быстро ⚡):
- **Задержка:** **0ms** (мгновенно)
- **Скорость:** ~50 отзывов за **1-2 секунды**
- Просто выбирается случайный шаблон из БД и вставляется

#### С ИИ-генерацией (медленнее 🐢):
- **Задержка:** **500ms** между каждым отзывом
- **Скорость:** ~50 отзывов за **25-30 секунд**
- Каждый ответ генерируется через API ИИ (Gemini)

### 4. Логика работы

#### Автоматический режим (auto):
1. Система проверяет настройки для рейтинга отзыва
2. Если включены шаблоны → выбирает случайный шаблон
3. Если шаблоны не включены → генерирует через ИИ
4. **Создаёт ответ со статусом `scheduled`** → сразу в очередь на отправку
5. Ответы автоматически публикуются через Chrome Extension (Ozon) или сервер (другие маркетплейсы)

#### Полуавтоматический режим (semi):
1. Система проверяет настройки для рейтинга отзыва
2. Если включены шаблоны → выбирает случайный шаблон
3. Если шаблоны не включены → генерирует через ИИ
4. **Создаёт ответ со статусом `drafted`** → черновик для проверки
5. Пользователь проверяет в разделе "Очередь модерации" и отправляет вручную

### 5. Массовая отправка (выбор отзывов галочкой)

Когда вы выбираете отзывы и нажимаете "Отправить":

**Выбор типа отправки:**
- **"Отправить шаблон"** → каждый отзыв заполняется случайным шаблоном и отправляется
- **"Отправить ответ ИИ"** → каждый отзыв генерируется через ИИ и отправляется

**Логика:**
1. Если есть черновик → черновик отправляется (статус → `scheduled`)
2. Если черновика нет:
   - При выборе "шаблон" → выбирается случайный шаблон → создаётся ответ → статус `scheduled`
   - При выборе "ИИ" → генерируется через ИИ → создаётся ответ → статус `scheduled`

### 6. Почему шаблоны быстрее?

**Шаблоны:**
- Просто SQL-запрос к БД: `SELECT * FROM reply_templates WHERE rating = X`
- Выбор случайного из результата
- Вставка в таблицу `replies`
- **Время:** ~20-50ms на отзыв

**ИИ-генерация:**
- HTTP-запрос к API ИИ (Gemini)
- Ожидание ответа от ИИ
- Обработка ответа
- Вставка в таблицу `replies`
- **Время:** ~500-2000ms на отзыв

### 7. Рекомендации

**Для быстрой обработки большого количества отзывов:**
1. ✅ Включите шаблоны для положительных отзывов (4-5 звёзд)
2. ✅ Используйте автоматический режим для 4-5 звёзд
3. ✅ Создайте 5-10 шаблонов для каждого рейтинга
4. ⚠️ Для негативных отзывов (1-2 звезды) лучше использовать ИИ для индивидуальных ответов

**Для контроля качества:**
1. ✅ Используйте полуавтоматический режим для всех рейтингов
2. ✅ Проверяйте черновики в "Очередь модерации"
3. ✅ Редактируйте шаблоны перед отправкой

## ⚙️ Технические детали

### Лимиты:
- **За один запуск:** до 50 отзывов
- **Задержка для ИИ:** 500ms между отзывами
- **Задержка для шаблонов:** 0ms (мгновенно)

### Статусы ответов:
- `drafted` - черновик (полуавтоматический режим)
- `scheduled` - в очереди на отправку (автоматический режим)
- `publishing` - отправляется сейчас
- `published` - отправлен
- `failed` - ошибка отправки

### Автоматическая отправка:
- **Ozon:** через Chrome Extension (каждую минуту проверяет очередь)
- **Другие маркетплейсы:** через серверный cron job (каждую минуту)

---

*Последнее обновление: 09.12.2025*

