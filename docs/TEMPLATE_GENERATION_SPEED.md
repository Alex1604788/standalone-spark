# Скорость заполнения отзывов шаблонами

## ⚡ Скорость обработки

### С шаблонами (быстро):
- **За один запуск cron job:** до **50 отзывов**
- **Задержка между отзывами:** **0ms** (мгновенно)
- **Время обработки 50 отзывов:** ~**2-5 секунд** (только время на запросы к БД)
- **За 5 минут:** до **50 отзывов** (один запуск)
- **За час:** до **600 отзывов** (12 запусков × 50 отзывов)

### С ИИ-генерацией (медленнее):
- **За один запуск:** до **50 отзывов**
- **Задержка между отзывами:** **500ms** (полсекунды)
- **Время обработки 50 отзывов:** ~**25-30 секунд**
- **За 5 минут:** до **50 отзывов**
- **За час:** до **600 отзывов**

## 📊 Как это работает:

1. **Cron job запускается каждые 5 минут**
2. **За один запуск обрабатывается до 50 неотвеченных отзывов**
3. **Для шаблонов:**
   - Выбирается случайный шаблон из БД (мгновенно)
   - Вставляется в ответ (мгновенно)
   - Сохраняется в БД (быстро)
   - **БЕЗ задержек**

4. **Для ИИ:**
   - Генерация через API (медленно)
   - Задержка 500ms между запросами
   - **С задержками**

## 🎯 Практический пример:

**Сценарий:** У вас 100 новых отзывов

**С шаблонами:**
- **1-й запуск (0:00):** обработано 50 отзывов за ~3 секунды
- **2-й запуск (0:05):** обработано оставшиеся 50 отзывов за ~3 секунды
- **Итого:** все 100 отзывов обработаны за **~6 секунд** (в два запуска)

**С ИИ:**
- **1-й запуск (0:00):** обработано 50 отзывов за ~30 секунд
- **2-й запуск (0:05):** обработано оставшиеся 50 отзывов за ~30 секунд
- **Итого:** все 100 отзывов обработаны за **~60 секунд** (в два запуска)

## ⚙️ Настройки:

- **Лимит за запуск:** 50 отзывов (можно изменить в коде)
- **Интервал cron job:** 5 минут (можно изменить в миграции)
- **Задержка для шаблонов:** 0ms (нет задержки)
- **Задержка для ИИ:** 500ms (можно изменить в коде)

## 📈 Оптимизация:

Если нужно обрабатывать больше отзывов быстрее:

1. **Увеличить лимит** (в `auto-generate-drafts/index.ts`, строка 135):
   ```typescript
   .limit(100); // вместо 50
   ```

2. **Уменьшить интервал cron job** (в миграции):
   ```sql
   '*/3 * * * *', -- каждые 3 минуты вместо 5
   ```

3. **Для шаблонов:** скорость уже максимальная (нет задержек)

## ⚠️ Важно:

- Если отзывов меньше 50, все обработаются за один запуск
- Если отзывов больше 50, они обработаются за несколько запусков
- Шаблоны работают **в разы быстрее** ИИ-генерации



