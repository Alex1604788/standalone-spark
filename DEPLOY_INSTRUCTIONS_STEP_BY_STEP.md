# 🚀 Инструкция по деплою sync-ozon-performance (ПОШАГОВО)

## ❗ ВАЖНО: Как понять, что код НЕ задеплоен

Если при синхронизации вы видите ошибку:
```json
{
  "chunk": 2,
  "total_chunks": 4  ← СТАРЫЙ КОД!
}
```

Это означает, что **старая версия все еще работает**!

**Правильный новый код** должен возвращать:
```json
{
  "chunks_processed": 1,
  "chunks_total": 34,
  "partial_sync": true  ← НОВЫЙ КОД!
}
```

---

## 📋 Шаг 1: Откройте Supabase Dashboard

1. Перейдите по ссылке:
   ```
   https://supabase.com/dashboard/project/nxymhkyvhcfcwjcfcbfy/functions
   ```

2. Найдите в списке функцию **`sync-ozon-performance`**

3. **Нажмите на неё** (не на три точки, а на саму строку)

---

## 📋 Шаг 2: Откройте редактор кода

После клика на функцию должен открыться **редактор кода**.

Вы увидите:
- Слева: навигация
- В центре: **код функции**
- Справа: настройки

---

## 📋 Шаг 3: Проверьте текущую версию

**ОЧЕНЬ ВАЖНО**: Прокрутите код до строки **~398** и найдите:

```typescript
const maxChunks = 1;  // ← Должна быть ЕДИНИЦА!
```

### ❌ Если там написано `const maxChunks = 4;` или этой строки вообще нет:
**Код старый, нужно заменить!**

### ✅ Если написано `const maxChunks = 1;`:
Проверьте строку **~365** (получение токена):

```typescript
const tokenResponse = await fetch("https://performance.ozon.ru/api/client/token", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    client_id: creds.client_id,
    client_secret: creds.client_secret,
    grant_type: "client_credentials",
  }),
  redirect: "manual",  // ← Эта строка ДОЛЖНА БЫТЬ!
});
```

### ❌ Если `redirect: "manual"` нет:
**Код старый, нужно заменить!**

---

## 📋 Шаг 4: Получите новый код

### Вариант A: Из GitHub (рекомендуется)

1. Откройте в **новой вкладке**:
   ```
   https://github.com/Alex1604788/standalone-spark/blob/claude/review-repository-015QiEMXVebETdBjXVAyQ98N/supabase/functions/sync-ozon-performance/index.ts
   ```

2. Нажмите кнопку **"Raw"** в правом верхнем углу (над кодом)

3. Откроется чистый текст кода

4. Нажмите **Ctrl+A** (выделить всё)

5. Нажмите **Ctrl+C** (скопировать)

### Вариант B: Из локального файла

1. Откройте файл в вашем редакторе:
   ```
   supabase/functions/sync-ozon-performance/index.ts
   ```

2. Выделите весь код (Ctrl+A)

3. Скопируйте (Ctrl+C)

---

## 📋 Шаг 5: Замените код в Supabase

Вернитесь на вкладку с **Supabase Dashboard**:

1. **Выделите ВЕСЬ старый код** в редакторе (Ctrl+A)

2. **Удалите** его (нажмите Delete или Backspace)

3. **Вставьте новый код** (Ctrl+V)

4. Проверьте, что код вставлен полностью:
   - Прокрутите в **самый низ** — должно быть `});` (закрывающая скобка serve)
   - Прокрутите в **самый верх** — должно начинаться с `import { serve }`

---

## 📋 Шаг 6: Сохраните и задеплойте

1. Найдите кнопку **"Deploy"** или **"Save"** (обычно справа сверху)

2. **Нажмите** на неё

3. Дождитесь сообщения **"Deployed successfully"** или **"Function deployed"**

   **⚠️ Если появилась ошибка компиляции**:
   - Проверьте, что весь код скопирован (должно быть **~595 строк**)
   - Проверьте, что не осталось старого кода
   - Попробуйте еще раз: удалить всё → вставить снова

---

## 📋 Шаг 7: Проверьте деплой

### Способ 1: Посмотрите на дату деплоя

В списке функций должна быть **свежая дата/время** деплоя (только что).

### Способ 2: Проверьте код

Откройте функцию снова и найдите строку **365**:

```typescript
redirect: "manual",  // ← Должно быть!
```

Если есть — **деплой успешен!** ✅

---

## 📋 Шаг 8: Протестируйте

1. Вернитесь в приложение: **Настройки → API OZON**

2. Нажмите **"За 7 дней"**

3. **Откройте DevTools** (F12) → вкладка **Network**

4. Посмотрите на ответ от функции

### ✅ Если видите:
```json
{
  "success": true,
  "message": "⚠️ Partial sync: processed 10 out of 340 campaigns",
  "partial_sync": true,
  "chunks_processed": 1
}
```
**Отлично! Новый код работает!**

### ❌ Если видите:
```json
{
  "error": "...",
  "chunk": 2,
  "total_chunks": 4
}
```
**Старый код все еще работает. Вернитесь к шагу 3.**

---

## 🆘 Если ничего не помогает

### Вариант 1: Пересоздайте функцию

1. **Удалите** функцию `sync-ozon-performance` в Supabase Dashboard

2. **Создайте новую** с тем же именем

3. Вставьте новый код

4. Deploy

### Вариант 2: Попросите показать скриншот

Сделайте скриншот:
- Редактора кода в Supabase (строка 365 и строка 398)
- Ошибки при деплое (если есть)

И покажите мне — я помогу разобраться!

---

## 📊 Как понять, что всё работает правильно

После успешного деплоя и запуска синхронизации вы должны увидеть **ОДНУ** из этих ситуаций:

### ✅ Вариант 1: Успешная синхронизация
```json
{
  "success": true,
  "message": "⚠️ Partial sync: processed 10 out of 340 campaigns. Run sync again to continue.",
  "partial_sync": true,
  "campaigns_total": 340,
  "campaigns_processed": 10,
  "campaigns_remaining": 330,
  "inserted": 42
}
```

### ⚠️ Вариант 2: Ошибка precision (нормально, исправим SQL)
```json
{
  "error": "Failed to save data",
  "details": "precision 0, scale 2 must round to absolute value less than 10^3"
}
```
Это означает, что функция работает, но нужно выполнить SQL fix.

### ❌ Вариант 3: Redirect loop (код не задеплоен!)
```json
{
  "error": "Fetch failed: Maximum number of redirects (20) reached"
}
```
Вернитесь к шагу 1 и проверьте код снова!

### ❌ Вариант 4: Старая ошибка с chunks (код не задеплоен!)
```json
{
  "error": "Failed to request performance report",
  "chunk": 2,
  "total_chunks": 4
}
```
Вернитесь к шагу 1 и проверьте код снова!

---

## 🎯 Финальная проверка

**После деплоя откройте функцию и проверьте эти 3 строки:**

1. **Строка ~53** (параметры polling):
   ```typescript
   maxAttempts: number = 6,
   initialDelay: number = 3000,
   pollInterval: number = 2000
   ```

2. **Строка ~365** (redirect для токена):
   ```typescript
   redirect: "manual",
   ```

3. **Строка ~369** (проверка строки ~369):
   ```typescript
   const maxChunks = 1;
   ```

Если **ВСЕ ТРИ** на месте — **код задеплоен правильно!** ✅

