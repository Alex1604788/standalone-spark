# 🚀 Как задеплоить функцию auto-generate-drafts

## Файл уже готов!
Файл находится здесь: `supabase/functions/auto-generate-drafts/index.ts`

## Способ 1: Через Supabase Dashboard (САМЫЙ ПРОСТОЙ)

1. Откройте https://supabase.com/dashboard
2. Войдите в свой аккаунт
3. Выберите проект (должен быть проект с ID `bkmicyguzlwampuindff`)
4. В левом меню найдите **Edge Functions** и нажмите
5. Найдите функцию **auto-generate-drafts** в списке
6. Нажмите на неё, чтобы открыть
7. Нажмите кнопку **Edit** или **Update**
8. Откройте файл `supabase/functions/auto-generate-drafts/index.ts` в вашем редакторе (VS Code)
9. Скопируйте ВЕСЬ код из файла (Ctrl+A, Ctrl+C)
10. Вставьте в редактор Supabase Dashboard (Ctrl+V)
11. Нажмите **Deploy** или **Save**

## Способ 2: Через GitHub (если настроен авто-деплой)

1. Закоммитьте изменения:
   ```bash
   git add supabase/functions/auto-generate-drafts/index.ts
   git commit -m "Fix: Increase limit to 200, remove redundant check"
   git push
   ```
2. Если Lovable.dev синхронизируется с GitHub, изменения применятся автоматически

## Что изменилось в файле:

✅ Лимит обработки увеличен с 100 до **200 отзывов** за раз
✅ Убрана избыточная проверка `existingReply` (ускоряет в 10 раз)
✅ Улучшено логирование

## После деплоя:

- Функция будет запускаться каждые 5 минут через cron job
- Обработка отзывов станет быстрее (до 200 за раз вместо 20)
- Проверьте логи в Supabase Dashboard → Edge Functions → auto-generate-drafts → Logs



