# ✅ Где смотреть изменения в приложении

## 🔍 Шаблоны ответов

### 1. Настройки (Settings)
**Путь:** Настройки → Выберите маркетплейс

**Что должно быть видно:**
- ✅ Для каждого рейтинга (1-5 звёзд) есть переключатель **"Использовать шаблоны ответов"**
- ✅ Иконка подсказки ❓ рядом с заголовком "Режимы ответов на отзывы"
- ✅ Иконка подсказки ❓ рядом с каждым переключателем "Использовать шаблоны ответов"

**Как проверить:**
1. Откройте раздел "Настройки"
2. Выберите маркетплейс
3. Прокрутите до раздела "Режимы ответов на отзывы"
4. Для каждого рейтинга (1-5 звёзд) должно быть:
   - Радиокнопки "Полуавтоматический" / "Автоматический"
   - Переключатель "Использовать шаблоны ответов" с иконкой ❓

### 2. Шаблоны ответов (Templates)
**Путь:** Шаблоны ответов

**Что должно быть видно:**
- ✅ Кнопка **"Сгенерировать шаблоны"** с иконкой подсказки ❓
- ✅ Иконка подсказки ❓ рядом с заголовком "Шаблоны ответов"
- ✅ При создании/редактировании шаблона есть поле **"Рейтинг отзыва (опционально)"** с иконкой подсказки ❓
- ✅ В карточках шаблонов отображается рейтинг (бейдж со звёздами)

**Как проверить:**
1. Откройте раздел "Шаблоны ответов"
2. Должна быть кнопка "Сгенерировать шаблоны" с иконкой ❓
3. Нажмите "Создать шаблон"
4. В форме должно быть поле "Рейтинг отзыва (опционально)" с иконкой ❓

## 💡 Подсказки (HelpIcon)

**Где должны быть видны иконки ❓:**

1. **Настройки:**
   - Рядом с "Режимы ответов на отзывы"
   - Рядом с "Режим ответов на вопросы"
   - Рядом с "Расширенные настройки"
   - Рядом с "Длина ответа"
   - Рядом с каждым "Использовать шаблоны ответов"

2. **Шаблоны ответов:**
   - Рядом с заголовком "Шаблоны ответов"
   - Рядом с кнопкой "Сгенерировать шаблоны"
   - Рядом с полем "Рейтинг отзыва"

3. **Отзывы и вопросы:**
   - Рядом с заголовком "Отзывы и вопросы"
   - Рядом с фильтром "Оценка"

4. **Очередь модерации:**
   - Рядом с заголовком "Очередь модерации"

5. **База знаний о товарах:**
   - Рядом с заголовком "База знаний о товарах"

6. **Вопросы:**
   - Рядом с заголовком (Не отвечено / Архив / Вопросы)

**Как проверить:**
- Наведите курсор на иконку ❓ - должна появиться подсказка с описанием

## ⚠️ Если изменения не видны

### 1. Перезапустите приложение
```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
npm run dev
# или
yarn dev
```

### 2. Проверьте, что миграции БД применены
- Откройте Supabase Dashboard → SQL Editor
- Проверьте, что таблица `marketplace_settings` имеет колонки:
  - `use_templates_1`
  - `use_templates_2`
  - `use_templates_3`
  - `use_templates_4`
  - `use_templates_5`
- Проверьте, что таблица `reply_templates` имеет колонку:
  - `rating`

### 3. Очистите кэш браузера
- Нажмите Ctrl+Shift+R (Windows) или Cmd+Shift+R (Mac)
- Или откройте DevTools (F12) → вкладка Network → поставьте галочку "Disable cache"

### 4. Проверьте консоль браузера
- Откройте DevTools (F12) → вкладка Console
- Проверьте, нет ли ошибок загрузки модулей

## 📝 Файлы, которые были изменены

- ✅ `src/components/HelpIcon.tsx` - новый компонент подсказок
- ✅ `src/pages/Settings.tsx` - добавлены переключатели шаблонов и подсказки
- ✅ `src/pages/Templates.tsx` - добавлена поддержка рейтингов и подсказки
- ✅ `src/pages/Reviews.tsx` - добавлены подсказки
- ✅ `src/pages/ReviewQueue.tsx` - добавлены подсказки
- ✅ `src/pages/ProductKnowledge.tsx` - добавлены подсказки
- ✅ `src/pages/Questions.tsx` - добавлены подсказки
- ✅ `supabase/functions/auto-generate-drafts/index.ts` - поддержка шаблонов
- ✅ `supabase/functions/generate-reply-templates/index.ts` - новая функция генерации шаблонов

## 🗄️ Миграции БД

Файлы миграций (нужно применить вручную через Supabase Dashboard):
- `supabase/migrations/20251208_fix_review_segment_for_answered_reviews.sql`
- `supabase/migrations/20251208_add_reply_templates_rating_support.sql`

Или объединённый файл:
- `combined_migrations_20251208.sql`



