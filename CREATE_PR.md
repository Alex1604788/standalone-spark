# Создание Pull Request для отката PromotionAnalytics

## Автоматическая ссылка для создания PR

Откройте эту ссылку в браузере:

```
https://github.com/Alex1604788/standalone-spark/compare/main...claude/auto-deploy-and-model-orders-hN0XE?expand=1
```

## Заполните поля PR:

### Title (Заголовок):
```
fix: Откатить PromotionAnalytics к рабочей версии с прямыми запросами
```

### Description (Описание):

```markdown
## Проблема

После применения SQL функций `get_campaign_performance_aggregated` и `get_product_performance_by_campaign` страница Аналитики Продвижения перестала отображать данные.

На странице показывается сообщение "Нет данных за выбранный период", хотя данные должны быть в базе.

## Решение

Откатил код к рабочей версии, которая использует прямые запросы к VIEW `ozon_performance_summary`.

## Изменения

- ✅ Откатил `PromotionAnalytics.tsx` к версии 8e704ed (до применения SQL функций)
- ✅ Вернул прямые запросы к `ozon_performance_summary`
- ✅ Восстановил дедупликацию расходов на уровне JavaScript
- ✅ Добавлены подробные логи для диагностики
- ✅ Добавлен диагностический скрипт `check-data-availability.mjs`

## Коммиты

- `aa668cc` - revert: Откатить PromotionAnalytics к версии с прямыми запросами
- `28c1a86` - chore: Добавить диагностический скрипт

## Тестирование

После мержа в main:
1. Обновить страницу Аналитика Продвижения
2. Открыть DevTools → Console
3. Проверить логи загрузки данных:
   - `✅ Загружено записей: ...`
   - `📊 Примеры кампаний: ...`
4. Убедиться, что кампании отображаются в таблице

## Ожидаемый результат

После мержа страница снова должна показывать данные аналитики продвижения с правильными расходами (дедуплицированными по дням).

## Примечание

SQL функции остаются в базе данных и миграциях. В будущем можно вернуться к их использованию после выяснения причины проблемы:
- Возможно, функции не были применены к production базе
- Возможно, есть проблема с правами доступа к RPC функциям
- Возможно, данные за выбранный период отсутствуют

## Файлы

- `src/pages/analytics/PromotionAnalytics.tsx` - откат к рабочей версии
- `check-data-availability.mjs` - новый диагностический скрипт
```

## После создания PR

1. Нажмите **Create pull request**
2. Дождитесь проверок CI/CD (если настроены)
3. Нажмите **Merge pull request**
4. Выберите **Squash and merge** или **Merge commit**
5. Подтвердите merge

## Результат

После мержа изменения попадут в `main` ветку и автоматически задеплоятся в production.
Страница Аналитики Продвижения снова начнёт показывать данные после обновления в браузере.
