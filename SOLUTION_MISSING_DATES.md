# 🎯 РЕШЕНИЕ: Отсутствуют дни в БД

## Диагноз

**Симптом:** За месяц показывает МЕНЬШЕ чем за неделю

**Причина:** В базе данных загружены НЕ ВСЕ дни декабря 2025

**Логика:**
```
Неделя (1-7 дек):  24 428 ₽  ✅
Месяц (1-31 дек):  < 24 428 ₽ ❌ НЕВОЗМОЖНО!
```

Если месяц меньше недели → в БД отсутствуют дни с 8 по 31 декабря (или их часть)

---

## Проверка (СРОЧНО!)

### Вариант 1: Через Supabase Dashboard (РЕКОМЕНДУЕТСЯ)

1. Откройте: https://supabase.com/dashboard/project/bkmicyguzlwampuindff/sql/new

2. Выполните запрос:
```sql
-- Проверка: сколько дней декабря есть в БД?
SELECT COUNT(DISTINCT stat_date) as days_count
FROM ozon_performance_daily
WHERE campaign_name = 'Кабель КГ 2*2,5'
  AND stat_date >= '2025-12-01'
  AND stat_date <= '2025-12-31';
```

**Ожидается:** 31 день
**Если меньше:** Проблема подтверждена!

3. Узнайте КАКИЕ даты отсутствуют:
```sql
WITH all_dates AS (
  SELECT generate_series(
    '2025-12-01'::date,
    '2025-12-31'::date,
    '1 day'::interval
  )::date as date
),
existing_dates AS (
  SELECT DISTINCT stat_date::date as date
  FROM ozon_performance_daily
  WHERE campaign_name = 'Кабель КГ 2*2,5'
    AND stat_date >= '2025-12-01'
    AND stat_date <= '2025-12-31'
)
SELECT a.date as missing_date
FROM all_dates a
LEFT JOIN existing_dates e ON a.date = e.date
WHERE e.date IS NULL
ORDER BY a.date;
```

---

## Решение

### Шаг 1: Запустить ручную синхронизацию

#### Способ A: Через Supabase Edge Functions

1. Откройте: https://supabase.com/dashboard/project/bkmicyguzlwampuindff/functions

2. Найдите функцию `sync-ozon-performance`

3. Запустите её с параметрами:
```json
{
  "marketplace_id": "<ВАШ_MARKETPLACE_ID>",
  "date_from": "2025-12-01",
  "date_to": "2025-12-31"
}
```

4. Дождитесь завершения (может занять несколько минут)

#### Способ B: Через cURL

```bash
curl -X POST \
  'https://bkmicyguzlwampuindff.supabase.co/functions/v1/sync-ozon-performance' \
  -H 'Authorization: Bearer <SERVICE_ROLE_KEY>' \
  -H 'Content-Type: application/json' \
  -d '{
    "marketplace_id": "<ВАШ_MARKETPLACE_ID>",
    "date_from": "2025-12-01",
    "date_to": "2025-12-31"
  }'
```

### Шаг 2: Проверить результат

После синхронизации повторите SQL запрос:
```sql
SELECT COUNT(DISTINCT stat_date) as days_count
FROM ozon_performance_daily
WHERE campaign_name = 'Кабель КГ 2*2,5'
  AND stat_date >= '2025-12-01'
  AND stat_date <= '2025-12-31';
```

Должно быть: **31 день**

### Шаг 3: Проверить расходы

```sql
SELECT ROUND(SUM(daily_expense)::numeric, 2) as total
FROM (
  SELECT stat_date, MAX(money_spent) as daily_expense
  FROM ozon_performance_daily
  WHERE campaign_name = 'Кабель КГ 2*2,5'
    AND stat_date >= '2025-12-01'
    AND stat_date <= '2025-12-31'
  GROUP BY stat_date
) as daily_unique;
```

Должно быть: **~109 130 ₽**

---

## Почему это произошло?

Возможные причины:

1. **Синхронизация не запускалась** для всех дней декабря
2. **API OZON не вернул данные** для некоторых дней
3. **Ошибка в Edge Function** при загрузке
4. **Лимиты API** - синхронизация прервалась

---

## Автоматизация (чтобы не повторялось)

### Вариант 1: Настроить CRON Job

Файл уже есть: `APPLY_CRON_JOB.sql`

```sql
-- Автоматическая синхронизация каждый день в 6:00 UTC
SELECT cron.schedule(
  'ozon-performance-daily-sync',
  '0 6 * * *',  -- каждый день в 6:00 UTC
  $$
  SELECT net.http_post(
    url:='https://bkmicyguzlwampuindff.supabase.co/functions/v1/sync-ozon-performance',
    headers:='{"Authorization": "Bearer <SERVICE_ROLE_KEY>", "Content-Type": "application/json"}'::jsonb,
    body:=concat('{"marketplace_id": "', marketplace_id, '", "days_back": 7}')::jsonb
  )
  FROM marketplaces WHERE marketplace = 'ozon';
  $$
);
```

### Вариант 2: GitHub Actions

Создать workflow который запускает синхронизацию каждый день.

---

## Файлы для использования

1. **CHECK_EXPENSES_QUICK.sql** - Быстрая проверка (скопируйте в Supabase)
2. **CRITICAL_EXPENSES_BUG_ANALYSIS.md** - Детальный анализ
3. **APPLY_CRON_JOB.sql** - Настройка автоматической синхронизации

---

## Чек-лист действий

- [ ] Выполнить SQL запрос проверки количества дней
- [ ] Узнать какие даты отсутствуют
- [ ] Запустить ручную синхронизацию для недостающих дат
- [ ] Проверить что все 31 день загружены
- [ ] Проверить что сумма расходов ~109 130 ₽
- [ ] Обновить страницу в приложении (Ctrl+Shift+R)
- [ ] Проверить что расходы показываются правильно
- [ ] Настроить автоматическую синхронизацию (CRON)

---

## Нужна помощь?

Покажите мне результаты SQL запросов:
1. Количество дней в БД
2. Список отсутствующих дат
3. Сумма расходов с дедупликацией

Или скриншот из Supabase Dashboard.

---

**Статус:** 🟡 ОЖИДАНИЕ СИНХРОНИЗАЦИИ
**Приоритет:** 🚨 ВЫСОКИЙ
**Время на исправление:** ~10 минут (после запуска синхронизации)
