# 🚀 Инструкция по применению SQL миграций

## Проблема
Аналитика продвижения загружала максимум 1000 записей из-за лимита Supabase.

## Решение
Применить SQL миграции, которые создают функции для агрегации данных на уровне БД.

## Шаги

### 1. Откройте Supabase Dashboard

Перейдите по ссылке:
**https://supabase.com/dashboard/project/bkmicyguzlwampuindff**

### 2. Откройте SQL Editor

В левом меню выберите **SQL Editor**

### 3. Создайте новый запрос

Нажмите кнопку **New Query**

### 4. Скопируйте SQL

Откройте файл `APPLY_SQL_NOW.sql` и скопируйте **ВСЁ** его содержимое

### 5. Вставьте и выполните

1. Вставьте SQL в редактор
2. Нажмите **RUN** или **Ctrl+Enter**
3. Дождитесь сообщения: **"Success. No rows returned"**

### 6. Проверьте результат

В том же SQL Editor выполните проверочный запрос:

\`\`\`sql
SELECT
  routine_name,
  routine_type
FROM information_schema.routines
WHERE routine_schema = 'public'
  AND routine_name IN (
    'get_campaign_performance_aggregated',
    'get_product_performance_by_campaign'
  );
\`\`\`

Должно вернуть **2 строки** ✅

## Готово!

После применения миграций:
- ✅ Функции созданы в БД
- ✅ Фронтенд автоматически начнёт их использовать
- ✅ Проблема с лимитом 1000 записей решена
- ✅ Все данные будут корректно агрегированы

## Файлы

- `APPLY_SQL_NOW.sql` - Готовый SQL для применения
- `ИСПРАВЛЕНИЕ_ПРОБЛЕМЫ_ЛИМИТА_1000_ЗАПИСЕЙ.md` - Подробное описание проблемы и решения

---

**Дата:** 2026-01-16
**Статус:** ✅ Готово к применению
