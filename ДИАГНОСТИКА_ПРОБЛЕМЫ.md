# Диагностика проблемы "ничего не работает"

## Что проверить

### 1. Консоль браузера (КРИТИЧНО!)

Откройте DevTools (F12) → Console и найдите логи:

```
🔍 Запрос данных для marketplace: [UUID]
📅 Период: 2025-12-01 - 2025-12-31
🔍 Проверка наличия данных: { checkData: ..., checkError: ... }
✅ Загружено записей: X
```

### Возможные сценарии:

#### Сценарий А: "Загружено записей: 0"
```
✅ Загружено записей: 0
⚠️ Нет данных в ozon_performance_summary для marketplace
```

**Причина:** Нет данных за выбранный период (01-31 декабря 2025)

**Решение:**
1. Нажмите кнопку "Расширить период до 180 дней"
2. Или вручную выберите другой период (например, последние 30 дней)

---

#### Сценарий Б: Ошибка "код = 42P01" (relation does not exist)
```
❌ Ошибка загрузки данных продвижений: { code: "42P01", message: "relation 'ozon_performance_summary' does not exist" }
```

**Причина:** VIEW `ozon_performance_summary` не существует в базе данных

**Решение:** Применить миграцию `20260112000000_create_ozon_performance_summary_view.sql`

---

#### Сценарий В: "Загружено записей: 1000"
```
✅ Загружено записей: 1000
✅ Сгруппировано кампаний: 15
📊 Примеры кампаний: [...]
```

**Причина:** Данные загружаются, но упираются в лимит 1000 записей

**Статус:** Это нормально для данной версии кода. Лимит будет решен после применения SQL функций.

---

#### Сценарий Г: Красная ошибка в консоли
```
Error: Failed to fetch
TypeError: NetworkError
```

**Причина:** Проблема с подключением к Supabase

**Решение:** Проверить:
- Интернет соединение
- URL Supabase в настройках
- API ключи

---

## 2. Какие данные доступны в базе?

### Запустите диагностический скрипт:

```bash
node check-data-availability.mjs
```

Скрипт покажет:
- Есть ли данные в ozon_performance_summary
- За какие даты доступны данные
- Минимальную и максимальную дату
- Примеры данных

---

## 3. Проверка деплоя

### Убедитесь, что изменения задеплоены:

1. Откройте страницу аналитики
2. Откройте DevTools → Sources
3. Найдите файл `PromotionAnalytics.tsx`
4. Проверьте, что в коде есть строка:
   ```
   // Загружаем данные по кампаниям
   ```
   А НЕ:
   ```
   // Загружаем данные по кампаниям используя SQL-функцию для агрегации
   ```

---

## Следующие шаги

1. **Сделайте скриншот консоли браузера** со всеми логами
2. **Напишите, какой период выбран** на странице
3. **Укажите, что именно показывается** на странице:
   - "Нет данных за выбранный период"?
   - Пустая таблица?
   - Ошибка?
   - Крутится загрузка бесконечно?

Это поможет точно определить причину проблемы.
